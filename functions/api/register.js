export async function onRequestPost(context){try{const{name,size,number}=await context.request.json();const cleanName=String(name||"").trim(),allowed=["S","M","L","XL","XXL"],jerseyNumber=Number(number);if(!cleanName||cleanName.length>50)return json({error:"Enter a valid name."},400);if(!allowed.includes(size))return json({error:"Select a valid size."},400);if(!Number.isInteger(jerseyNumber)||jerseyNumber<0||jerseyNumber>99)return json({error:"Jersey number must be between 0 and 99."},400);await context.env.DB.prepare("INSERT INTO registrations (name,size,jersey_number) VALUES (?,?,?)").bind(cleanName,size,jerseyNumber).run();return json({success:true},201)}catch(e){console.error(e);return json({error:"Something went wrong. Try again."},500)}}function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{"Content-Type":"application/json"}})}